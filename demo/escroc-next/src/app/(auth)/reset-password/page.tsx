import { ResetPasswordForm } from "@/components/forms/ResetPasswordForm";

export const metadata = { title: "Reset Password — Escroc" };

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
