import { ExchangeCrypto } from "@/components/dashboard/page/ExchangeCrypto";

export const metadata = {
  title: "Exchange Crypto — AdCrypto",
};

export default function ExchangeCryptoPage() {
  return <ExchangeCrypto />;
}
