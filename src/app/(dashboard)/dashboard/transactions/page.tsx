import { Transactions } from "@/components/dashboard/page/Transactions";

export const metadata = {
  title: "Transactions — AdCrypto",
};

export default function TransactionsPage() {
  return <Transactions />;
}
