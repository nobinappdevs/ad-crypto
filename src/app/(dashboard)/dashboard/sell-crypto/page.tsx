import { SellCrypto } from "@/components/dashboard/page/SellCrypto";

export const metadata = {
  title: "Sell Crypto — AdCrypto",
};

export default function SellCryptoPage() {
  return <SellCrypto />;
}
