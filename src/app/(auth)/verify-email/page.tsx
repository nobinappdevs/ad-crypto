import { VerifyEmailForm } from "@/components/forms/VerifyEmailForm";

export const metadata = {
  title: "Verify Email — AdCrypto",
};

export default function VerifyEmailPage() {
  return <VerifyEmailForm />;
}
