import { Verify2faForm } from "@/components/forms/Verify2faForm";

export const metadata = {
  title: "Two-Factor Verification — AdCrypto",
};

export default function Verify2faPage() {
  return <Verify2faForm />;
}
